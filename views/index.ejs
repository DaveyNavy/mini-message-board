<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link rel="stylesheet" href="/styles.css">
</head>
<body>
    <h1><%= title %></h1>
    <a href="/new">New Message</a>
    <% messages.forEach((message, i) => { %>
        <h3><%= message.text %></h3>
        <p><%= message.user %></p>
        <p><%= message.added %></p>
        <a href="<%= i %>">More Details</a>
    <% }) %>
</body>
</html>